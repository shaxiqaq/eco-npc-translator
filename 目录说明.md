# 目录说明

## 常用入口

- `启动伤害Overlay.cmd`：伤害悬浮窗，已包含控制台完整统计。
- `采集伤害包.cmd`：伤害包采集/调试。
- `启动NPC翻译.cmd`：NPC 对话翻译。
- `配置翻译.cmd`：翻译配置界面。
- `对齐词库.cmd`：把采集语料对齐进缓存。

## 伤害统计相关

- `eco_damage_overlay.py`：透明悬浮窗入口。
- `eco_damage_meter.py`：伤害统计核心逻辑，被悬浮窗复用。
- `eco_damage_capture.py`：伤害包采集器。
- `_damage_capture.js`：Frida 抓包脚本。
- `damage_overlay_config.json`：悬浮窗位置配置。
- `skill_names.json`：技能 ID 到技能名的映射；没有配置时显示 `技能#ID`。
- `mob_names.json`：怪物模板 ID 到怪物名的映射；没有配置时显示 `怪物#ID`。
- `导入客户端名称表.cmd`：从客户端导出的怪物/技能 CSV 自动生成 `mob_names.json` 和 `skill_names.json`。
- `本地翻译怪物名称.cmd`：用本地 Ollama 模型把 `mob_names.json` 的日文怪物名批量翻译成中文。

## NPC 翻译相关

- `eco_npc_mitm.py`：NPC 翻译主程序。
- `eco_settings.py`：配置工具。
- `_mitm.js`：NPC 翻译 Frida 脚本。
- `screen_translator/`：翻译服务适配模块。
- `cache_sync.py`：共享词库同步。

## 词库/采集数据

- `npc_cache.json`：运行时查表缓存。
- `npc_seen.json`：见过的英文语料。
- `harvest.jsonl`、`harvest_dict.json`：采集数据。
- `npc_dict.json`：官方/仓库字典。
- `bridge_cache.json`：对齐时的机翻缓存。
- `sync_cursor.json`：共享词库拉取游标。

## 其他目录

- `logs/`：新运行日志。旧日志已归档。
- `archive/`：旧实验脚本、旧日志和参考资料。
- `archive/runtime_logs_20260630/`：本次整理前的运行日志。
- `dict_node/`：共享词库 Cloudflare Worker 节点。
- `docs/`：分发说明等文档。
