# Legacy renderer (deprecated)

This folder is the **pre-React** HTML/CSS/JS UI.

The shipped app loads `electron/renderer/` (Vite + React build from `electron/ui/`).

Do not edit files here for product work unless you are recovering a legacy layout reference.
Kept only for historical comparison; safe to delete after confirming no scripts depend on it.
